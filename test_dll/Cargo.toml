[package]
name = "test_dll"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[features]
no_gui = []

[dependencies]
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_Threading",
    "Win32_UI_WindowsAndMessaging",
] }
