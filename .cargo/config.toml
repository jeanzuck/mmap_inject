# Equivalent to /MT — statically link MSVC CRT.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static", "-C", "link-arg=/DYNAMICBASE"]

[alias]
r = ["run", "--release"]
b = ["build", "--release"]
